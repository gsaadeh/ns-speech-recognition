<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="onPageLoaded">
  <DockLayout stretchLastChild="false">
  <StackLayout dock="top">
    <Label text="Speech Recognition Demo" class="title"/>
    <Label class="message" text="{{ speechText }}" />
  </StackLayout>
  <StackLayout dock="bottom">
      <Button id="startRecording" text="{{ recordButtonText }}" isEnabled="{{ recordButtonEnabled }}" tap="recordButtonTapped" />
  </StackLayout>
</DockLayout>
</Page>
